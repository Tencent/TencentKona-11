Vector API Benchmarks

This suite contains a set of microbenchmarks automatically derived from unit tests.

1. (Re-)Generate benchmarks
  $ cd ..; bash gen-tests.sh jmh

2. Build
  $ mvn install

3. Run benchmarks
  $ java -jar target/vector-benchmarks.jar '.*'
