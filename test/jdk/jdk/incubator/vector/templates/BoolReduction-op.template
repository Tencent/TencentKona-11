    @Test(dataProvider = "boolUnaryOpProvider")
    static void [[TEST]]$vectorteststype$(IntFunction<boolean[]> fm) {
        boolean[] mask = fm.apply(SPECIES.length());
        boolean[] r = new boolean[mask.length];

        for (int ic = 0; ic < INVOC_COUNT; ic++) {
            for (int i = 0; i < mask.length; i += SPECIES.length()) {
              Vector.Mask<$Wideboxtype$, Shapes.S$bits$Bit> vmask = SPECIES.maskFromArray(mask, i);
              r[i] = vmask.[[TEST]]();
            }
        }

        assertReductionBoolArraysEquals(mask, r, $vectorteststype$::[[TEST]]);
    }
